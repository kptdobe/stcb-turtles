version: 1

indices:
  turtles:
    include:
      - '/turtles/*'
    target: /turtles-index
    properties:
      id:
        select: body h1
        value: |
          textContent(el)
      observations:
        select: body .observation
        values: |
          innerHTML(el)